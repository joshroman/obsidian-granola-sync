// Re-export from structured-logger for backward compatibility
export { Logger, StructuredLogger } from './structured-logger';
export type { LogLevel, LogContext, LogEntry, LogMetrics } from './structured-logger';